[pytest]
testpaths = tests
markers =
    hello: mark test as a hello world test
    integration: mark test as an integration test requiring external dependencies
    llm: mark test as requiring a real LLM connection (may be slow or flaky)
    slow: mark test as slow running